; This file is loaded at boot time by devious and processed as a scheme source
; file.
;
; The valid values are shown below commented out, however they are asserted by
; the code and so you should only change them if you need to to gaurantee
; forward compatibility
;
;
;(define devious-server  "irc.psych0tik.net")
;(define devious-port    6697)
;(define devious-ssl?    #t)
;
;(define devious-nick    "devious")
;(define devious-name    "devious")
;
;(define devious-channels (list "#hackery"))
